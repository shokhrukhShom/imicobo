# Copy this file to ".env" on the server and fill in your real key.
# The .env file is git-ignored so your key never leaves the server.
YT_API_KEY=paste_your_free_youtube_data_api_key_here
PORT=8080
CACHE_FILE=/opt/imicobo/yt_cache.json
